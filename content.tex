\section{section}

\greenbox{\subsection[]{subsection in a greenbox}}{text in a box}

\subsection{subsection without a box}
\bluebox{bluebox}{look}
\redbox{redbox}{at that!}

lognormal:

$$
f(x)=\frac{1}{\sqrt{2\pi}\sigma x}\,\exp\left(-\,\frac{(\ln(x)-\mu)^{2}}{2\sigma^{2}}\right) 
$$

\begin{equation}
    \sin{x^2}
\end{equation}

python:
\begin{lstlisting}
    import numpy as np
    x = np.arange(10) #this is an array
    print(x)
\end{lstlisting}